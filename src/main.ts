// import { initHome } from './home';
// import { initAbout } from './about';
// import { initContact } from './contact';

// const container = document.getElementById('canvas-container');
// if (!container) {
//     console.error("Container not found! Ensure there's an element with ID 'canvas-container'.");
// }

// function showPage(page: string) {
//     if (!container) return;

//     container.innerHTML = ''; // Очистка контейнера
//     console.log(`Переключаемся на страницу: ${page}`); // Лог для отслеживания

//     switch (page) {
//         case 'home':
//             initHome(container);
//             break;
//         case 'about':
//             initAbout(container);
//             break;
//         case 'contact':
//             initContact(container);
//             break;
//         default:
//             initHome(container);
//     }
// }

// // Загрузка страницы Home по умолчанию
// showPage('home');
